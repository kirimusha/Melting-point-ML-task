https://www.kaggle.com/competitions/spbstu-polyml-competition

Температура плавления — одно из фундаментальных физических свойств вещества, критически важное для множества научных и промышленных приложений, от разработки новых лекарств до создания современных материалов. Однако экспериментальное измерение этого параметра может быть долгим и дорогостоящим процессом. Могут ли модели искусственного интеллекта помочь нам в этом?

🧪 Context
Температура плавления ($T_m$) — одно из фундаментальных физических свойств вещества, критически важное для множества научных и промышленных приложений, от разработки новых лекарств до создания современных материалов. Экспериментальное измерение $T_m$ может быть долгим и дорогостоящим, а базы данных, хотя и существуют, часто неполны.

Можем ли мы, зная только структуру молекулы (SMILES), предсказать, при какой температуре вещество перейдет из твердого состояния в жидкое?

Ваша задача — создать высокоинтерпретируемую модель машинного обучения для предсказания температуры плавления (Tm) химических соединений, используя датасет Bradley Double Plus Good Melting Point.


Dataset Description

В этом соревновании вам предлагается решить задачу регрессии: предсказать температуру плавления химических соединений на основе их молекулярной структуры. Данные основаны на открытых датасетах Jean-Claude Bradley Open Melting Point Dataset.

Вам предоставлены обучающая выборка с известными температурами плавления и тестовая выборка, для которой необходимо сделать прогноз.

Файлы

train.csv — обучающая выборка. Содержит строковое представление молекулы (SMILES) и целевую переменную (температуру плавления).

test.csv — тестовая выборка. Содержит идентификатор (id) и SMILES. Для этого набора данных вы должны предсказать целевую переменную.

sample_submission.csv — пример файла сабмишна в правильном формате.

Описание полей

id — уникальный идентификатор примера (присутствует только в тестовой выборке).

smiles — (Simplified Molecular Input Line Entry System) строковое представление химической структуры молекулы. Используя специализированные библиотеки (например, RDKit), из этой строки можно извлечь физико-химические дескрипторы (молекулярный вес, количество водородных связей и т.д.) или построить граф молекулы.

mpC — Целевая переменная. Температура плавления в градусах Цельсия (°C).


Особенности подготовки данных


Данные были очищены от записей с пропущенными значениями.

Разбиение на train/test произведено случайным образом (random split).


Полезная информация

Для работы с химическими данными (преобразования SMILES в числовые признаки) рекомендуется использовать библиотеку RDKit.

В качестве базовых признаков (дескрипторов), которые можно извлечь из SMILES, часто используются:


MolWt: Молекулярная масса.

TPSA: Топологическая полярная площадь поверхности.

MolLogP: Коэффициент распределения октанол-вода (мера гидрофильности/липофильности).

NumHDonors / NumHAcceptors: Количество доноров и акцепторов водородных связей.
